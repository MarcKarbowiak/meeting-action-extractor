{
  "name": "@meeting-action-extractor/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint \"src/**/*.ts\" \"test/**/*.ts\""
  },
  "dependencies": {
    "@meeting-action-extractor/db": "workspace:*",
    "fastify": "^5.6.1",
    "zod": "^4.1.11"
  }
}
