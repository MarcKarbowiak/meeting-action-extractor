{
  "name": "@meeting-action-extractor/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "serve": "tsx src/index.ts",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint \"src/**/*.ts\" \"test/**/*.ts\""
  },
  "dependencies": {
    "@fastify/cors": "^10.0.1",
    "@meeting-action-extractor/db": "workspace:*",
    "@meeting-action-extractor/shared": "workspace:*",
    "fastify": "^5.6.1",
    "zod": "^4.1.11"
  }
}
